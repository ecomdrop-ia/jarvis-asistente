"use strict";

// Reserved for future Node ↔ renderer bridge APIs (e.g., exposing system
// signals like presence detection, native notifications, file picker).
// Currently empty — the renderer uses standard web APIs only.
